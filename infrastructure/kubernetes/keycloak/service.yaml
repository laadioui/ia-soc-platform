apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: ai-soc
  labels:
    app.kubernetes.io/name: keycloak
    app.kubernetes.io/part-of: ai-soc-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: keycloak
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
